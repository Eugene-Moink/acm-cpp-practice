#include <bits/stdc++.h>
using namespace std;
using ll = long long;

void solve()
{
    int x;
    cin >> x;
    if (x == 1)
        cout << 2;
    else if (x == 2)
        cout << 3;
    else
        cout << 2;
}

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    solve();
    return 0;
}