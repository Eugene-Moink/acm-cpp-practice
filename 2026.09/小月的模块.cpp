#include <bits/stdc++.h>
using namespace std;
using ll = long long;

void solve()
{
    int s, a, b;
    cin >> s >> a >> b;
    if (s == 0)
        cout << a;
    else
        cout << b;
}

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    solve();
    return 0;
}